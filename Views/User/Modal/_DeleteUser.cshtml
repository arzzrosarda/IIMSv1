@{
    AccountUser accUser = ViewBag.User;
}
    <div id="modal-delete-user" class="modal zoomIn" tabindex="-1" aria-labelledby="modal-delete-user" >
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-light pt-3 pb-3">
                    <h4 class="modal-title"><i class="fas fa-exclamation-triangle " style="font-size: 25px;"></i> &nbsp; Delete</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                        
                    </button>
                </div>
                <div class="modal-body p-5">
                    Deleting a user account will also delete all associated employee data and system logs, and may introduce breaking changes to the database.
                    This action is not reversible, therefore, continuing is <b>NOT RECOMMENDED</b>.
                    <br /><br />
                    Only continue with this action if you are ABSOLUTELY certain that no other data will be negatively affected.
                    <br /><br />
                    Do you wish to delete this user account anyway?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-light" data-bs-dismiss="modal">Cancel</button>&nbsp;
                    <a type="button" href="@Url.Action("_Delete", "User", new {UserId = accUser.Id})" class="btn btn-danger"><i class="fa fa-trash"></i> Delete</a>
                </div>
            </div>
        </div>
    </div>