@{
    var Path = Context.Request.Path;
    var QueryStrings = Context.Request.QueryString;
    var returnURl = Path + QueryStrings;
}
<form asp-action="_DeleteItem" asp-controller="Item">
    <div id="modal-delete-item" class="modal fade" tabindex="-1" aria-labelledby="modal-delete-item" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-light pt-3 pb-3">
                    <h4 class="modal-title"><i class="fas fa-exclamation-triangle " style="font-size: 25px;"></i> &nbsp; Delete Item</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                        
                    </button>
                </div>
                <div class="modal-body p-5">
                    <input class="form-select" type="hidden" name="returnUrl" value="@returnURl" />
                    <input class="form-select" type="hidden" name="ItemId" id="ItemIdDelete" />
                    Deleting an item will also delete all associated data.
                    This action is not reversible, therefore, continuing is <b>NOT RECOMMENDED</b>.
                    <br /><br />
                    Only continue with this action if you are ABSOLUTELY certain about deleting this item.
                    <br /><br />
                    Do you wish to delete this item anyway?
                </div>
                <div class="modal-footer">
                    <button type="reset" class="btn btn-light" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-danger btndeleteItem"><i class="fa fa-trash"></i> Delete</button>
                </div>
            </div>
        </div>
    </div>
</form>