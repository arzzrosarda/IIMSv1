@{
    var Path = Context.Request.Path;
    var QueryStrings = Context.Request.QueryString;
    var returnURl = Path + QueryStrings;
}
<form asp-action="_DisableDept" asp-controller="Department">
    <div id="modal-disable-dept" class="modal zoomIn" tabindex="-1" aria-labelledby="modal-disable-dept" >
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header bg-light pt-3 pb-3">
                    <h5 class="modal-title"><i class="ri-lg ri-error-warning-line" style="font-size: 25px;"></i> &nbsp; DISABLED DEPARTMENT </h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close">
                        
                    </button>
                </div>
                <div class="modal-body">
                    <input class="form-select" type="hidden" name="returnUrl" value="@returnURl" />
                    <input class="form-select" type="hidden" name="DeptId" id="DeptIdDisable"/>
                    Disabling an deparment will also disable all associated data 
                    and user accounts and won't be able to change or access the information needed.
                    <br /><br />
                    Only continue with this action if you are ABSOLUTELY certain about disabling this department for now,
                    you may activate it again in your on department page.
                    <br /><br />
                    Do you wish to disable this department anyway?
                </div>
                <div class="modal-footer">
                    <buton class="btn btn-light waves-effect waves-light" type="reset" data-bs-dismiss="modal">Cancel</buton>
                    <button type="submit" class="btn btn-danger waves-effect waves-light">Disable</button>
                </div>
            </div>
        </div>
    </div>
</form>
